spring:
  application:
    name: Practise
  datasource:
    url: ${DATABASE_URL}
    username: ${DATABASE_USERNAME}
    password: ${DATABASE_PASSWORD}
  sql:
    init:
      mode: always

server:
  port: ${PORT:8080}
  address: 0.0.0.0
